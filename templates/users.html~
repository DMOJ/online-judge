{% extends "base.html" %}
{% block title %}Users{% endblock %}

{% block body %}

<h1 align="center">Users</h1>

<!-- based off of http://www.w3schools.com/html/html_tables.asp -->
<style>
table.users {
	border-collapse:collapse;
	width:100%;
	margin-left: auto;
        margin-right: auto;
}

table.users tr:nth-child(odd)	{background-color:#F6F4F0;}
table.users tr:nth-child(even)	{background-color:#FFFFFF;}

table.users th {
	color:#ffffff;
	background-color:#555555;
	border:1px solid #555555;
	padding:3px;
	vertical-align:top;
	text-align:center;
}

table.users td {
	border:1px solid #d4d4d4;
	padding:5px;
	padding-top:7px;
	padding-bottom:7px;
	vertical-align:center;
	text-align:center;
}

</style>
<table class="users" style="width:70%">
    <tr>
        <th>Rank</th>
        <th>User</th>
        <th>Points</th>
        <th>Problems</th>
    </tr>
    {% for user in users %}
    <tr>
        <td>0</td>
        <td><a href="./user/{{ user.user.username }}">{{ user.user.username }} ({{ user.name }})</a></td>
        <td>0</td>
        <td>0</td>
    </tr>
    {% endfor %}
</table>

{% endblock %}
