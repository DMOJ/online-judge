- spaceless
each name, versions in runtime_versions
    if versions
        if versions.0 != versions|last
            | #{name} #{versions.0} - #{versions|last}
        else
            | #{name} #{versions.0}
    else
        = name
    if not forloop.last
        = ', '
- endspaceless