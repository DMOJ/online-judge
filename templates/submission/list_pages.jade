.pagination
    if submissions.has_previous
        a(href='{{ submissions.previous_page_number }}') <<
    
    each page in submissions.page_range
        if not page
            | ...
        else
            if page == submissions.number
                a.active-page(href=page) #{page}
            else
                a(href=page) #{page}
    if submissions.has_next
        a(href='{{ submissions.next_page_number }}') >>