extends base
- load markdown
- load staticfiles
- load reference

block title
    | #{flatpage.title} - #{SITE_LONG_NAME}

block og_title
    meta(property='og:title', content='{{ flatpage.title }} - {{ SITE_LONG_NAME }}')

block content_title
    | #{flatpage.title}

block header
    include flatpages/admin_link

block body
    .content-description
        | #{flatpage.content|markdown:"solution"|reference}
