- load i18n

h3 {% trans "Statistics" %} #[i.fa.fa-pie-chart]
.sidebox-content.sidebox-table: table.table
    each key, cls, count in results
        tr(class=cls)
            td(style='font-weight:bold;text-align:left')= key
            td(style='color:black')= count
